fn print(comptime fmt: []u8, args: anytype) void

print("{}, {d}\n", .{ a, b });

fn print<fmt: []u8, args_type: ComptimeList[type]>

print<"{}, {d}\n", <Typeof!(a), Typeof!(b)>>(a, b);

print<"{}, {d}\n"> -> fn (^Display, ^Num) void

fn stringify(data: anytype, 
             option: StringifyOption,
) !String {}

let result = stringify(A { .buffer: "this", .option: null })!;

